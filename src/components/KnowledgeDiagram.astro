<figure class="knowledge-diagram" aria-labelledby="diagram-title diagram-desc">
  <figcaption>
    <span class="meta">Visual artifact / public reconstruction</span>
    <strong id="diagram-title">From fragmented knowledge to useful context</strong>
    <span id="diagram-desc">A generalized view of the retrieval experiment. It does not reproduce internal systems or data.</span>
  </figcaption>
  <div class="diagram-flow">
    <div class="source-stack" aria-label="Fragmented sources">
      <span>Known issue</span><span>Team note</span><span>Ownership</span>
    </div>
    <span class="connector" aria-hidden="true">→</span>
    <div class="diagram-node blue-node"><span class="meta">01 / Curate</span><strong>Structured<br />knowledge</strong></div>
    <span class="connector" aria-hidden="true">→</span>
    <div class="diagram-node"><span class="meta">02 / Retrieve</span><strong>Relevant<br />context</strong></div>
    <span class="connector" aria-hidden="true">→</span>
    <div class="diagram-node output"><span class="meta">03 / Assist</span><strong>Grounded<br />response</strong></div>
  </div>
  <p class="meta limitation">Prototype boundary: the experiment tested retrieval. Governance, contribution review, and long-term maintenance remained open design questions.</p>
</figure>

<style>
  .knowledge-diagram { margin: 0; padding: clamp(1.25rem, 3vw, 2.5rem); color: var(--ink); border: 1px solid var(--rule); background: var(--paper-bright); }
  figcaption { display: grid; grid-template-columns: .7fr 1.5fr 1fr; gap: 2rem; padding-bottom: 2rem; border-bottom: 1px solid var(--rule); }
  figcaption strong { font-size: clamp(1.4rem, 2.4vw, 2.3rem); font-weight: 500; line-height: 1.05; letter-spacing: -.04em; }
  figcaption > span:last-child { color: var(--muted); font-size: .9rem; }
  .diagram-flow { display: grid; grid-template-columns: 1fr auto 1.2fr auto 1.2fr auto 1.2fr; gap: clamp(.5rem, 1.4vw, 1.5rem); align-items: center; padding: clamp(3rem, 8vw, 7rem) 0; }
  .source-stack { display: grid; gap: .4rem; transform: rotate(-2deg); }
  .source-stack span { padding: .65rem .8rem; border: 1px solid var(--rule); background: var(--paper); font-family: var(--mono); font-size: .7rem; letter-spacing: .05em; text-transform: uppercase; }
  .source-stack span:nth-child(2) { transform: translateX(.5rem); }
  .source-stack span:nth-child(3) { transform: translateX(-.25rem); }
  .diagram-node { display: flex; min-height: 10rem; flex-direction: column; justify-content: space-between; padding: 1rem; border: 1px solid var(--ink); }
  .diagram-node strong { font-size: clamp(1rem, 1.7vw, 1.4rem); font-weight: 500; line-height: 1.15; }
  .blue-node { color: white; border-color: var(--blue); background: var(--blue); }
  .blue-node .meta { color: white; }
  .output { border-width: 2px; }
  .connector { color: var(--blue); font-size: 1.5rem; }
  .limitation { margin: 0; padding-top: 1rem; border-top: 1px solid var(--rule); color: var(--muted); }
  @media (max-width: 760px) {
    figcaption { grid-template-columns: 1fr; gap: .75rem; }
    .diagram-flow { grid-template-columns: 1fr; padding-block: 2rem; }
    .connector { transform: rotate(90deg); justify-self: center; }
    .source-stack { width: 80%; justify-self: center; }
    .diagram-node { min-height: 7rem; }
  }
</style>
